name: Corepy CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["**"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0

jobs:
  test:
    name: ${{ matrix.os }} / Py${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-suffix: "py${{ matrix.python-version }}"

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable

      # ========================================
      # STEP 1: Platform Dependencies
      # ========================================
      - name: Install Dependencies (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libopenblas-dev cmake ninja-build

      - name: Install Dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install openblas cmake ninja
          echo "CMAKE_PREFIX_PATH=$(brew --prefix openblas)" >> $GITHUB_ENV
          echo "PKG_CONFIG_PATH=$(brew --prefix openblas)/lib/pkgconfig" >> $GITHUB_ENV
          echo "LIBRARY_PATH=$(brew --prefix openblas)/lib:$LIBRARY_PATH" >> $GITHUB_ENV

      - name: Install Dependencies (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          curl -L -o openblas.zip https://github.com/OpenMathLib/OpenBLAS/releases/download/v0.3.28/OpenBLAS-0.3.28-x64.zip
          unzip -q openblas.zip -d openblas
          echo "OPENBLAS_DIR=${{ github.workspace }}/openblas" >> $GITHUB_ENV
          echo "CMAKE_PREFIX_PATH=${{ github.workspace }}/openblas" >> $GITHUB_ENV
          # Add OpenBLAS bin to PATH so DLLs are found at runtime
          echo "${{ github.workspace }}/openblas/bin" >> $GITHUB_PATH

      # ========================================
      # STEP 2: Install pybind11 (required for CMake)
      # ========================================
      - name: Install pybind11
        shell: bash
        run: |
          uv venv
          uv pip install pybind11

      # ========================================
      # STEP 3: Build C++ Kernels (csrc/)
      # ========================================
      - name: Build C++ kernels
        shell: bash
        run: |
          # Detect pybind11 CMake directory
          PYBIND11_CMAKE_DIR=$(uv run python -c "import pybind11; print(pybind11.get_cmake_dir())")
          echo "pybind11 found at: $PYBIND11_CMAKE_DIR"
          
          if [ "$RUNNER_OS" == "Windows" ]; then
            cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -Dpybind11_DIR="$PYBIND11_CMAKE_DIR"
          else
            cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -Dpybind11_DIR="$PYBIND11_CMAKE_DIR"
          fi
          cmake --build build --config Release
          
          if [ "$RUNNER_OS" == "macOS" ]; then
             echo "Compiling Metal Kernels..."
             xcrun -sdk macosx metal -v -c csrc/src/metal/kernels.metal -o build/csrc/kernels.air
             xcrun -sdk macosx metallib build/csrc/kernels.air -o build/csrc/default.metallib
             echo "COREPY_METAL_LIB_PATH=$(pwd)/build/csrc/default.metallib" >> $GITHUB_ENV
          fi

      # ========================================
      # STEP 4: Install Python deps & Build Rust
      # ========================================

      - name: Install Python dependencies
        shell: bash
        run: |
          # Install dependencies WITHOUT triggering editable install yet
          # This prevents maturin from building before C++ kernels are ready
          uv sync --no-install-workspace --all-extras --group dev

      - name: Build Rust extension
        shell: bash
        run: |
          # Now build Rust extension with CMake artifacts present
          uv run maturin develop --release --manifest-path rust/core/Cargo.toml

      # ========================================
      # STEP 5: Verify & Test
      # ========================================
      - name: Verify Import
        shell: bash
        run: |
          uv run python -c "import corepy; print(f'corepy {corepy.__version__}')"
          uv run python -c "from corepy import _corepy_rust; print('Rust runtime loaded')"

      - name: Run Tests
        shell: bash
        run: |
          uv run pytest tests/ --cov=corepy --cov-report=term -v

  # ===========================================================================
  # Rust Compile Check (verifies code compiles for all target architectures)
  # ===========================================================================
  rust-check:
    name: Rust Check / ${{ matrix.target }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          # x86_64 targets
          - target: x86_64-unknown-linux-gnu
            features: ""
          # ARM64 cross-compile (no execution, just verify compilation)
          - target: aarch64-unknown-linux-gnu
            features: ""

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation tools
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libopenblas-dev cmake ninja-build python3 python3-pip

      - name: Install pybind11
        run: |
          pip3 install pybind11

      - name: Build C++ kernels
        run: |
          PYBIND11_CMAKE_DIR=$(python3 -c "import pybind11; print(pybind11.get_cmake_dir())")
          cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -Dpybind11_DIR="$PYBIND11_CMAKE_DIR"
          cmake --build build --config Release

      - name: Cargo Check (compile only)
        run: |
          cargo check --manifest-path rust/core/Cargo.toml --target ${{ matrix.target }}

  # ===========================================================================
  # Rust Lints (Clippy)
  # ===========================================================================
  lint:
    name: Rust Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libopenblas-dev cmake ninja-build python3 python3-pip

      - name: Install pybind11
        run: |
          pip3 install pybind11

      - name: Build C++ kernels
        run: |
          PYBIND11_CMAKE_DIR=$(python3 -c "import pybind11; print(pybind11.get_cmake_dir())")
          cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -Dpybind11_DIR="$PYBIND11_CMAKE_DIR"
          cmake --build build --config Release

      - name: Clippy
        run: |
          cargo clippy --manifest-path rust/core/Cargo.toml -- -D warnings --allow clippy::missing_safety_doc

  # ===========================================================================
  # Rust Format Check
  # ===========================================================================
  fmt:
    name: Rust Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Check format
        run: |
          cargo fmt --manifest-path rust/core/Cargo.toml -- --check

